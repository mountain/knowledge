(ns properties
   (:refer-clojure :exclude [==])
   (:use clojure.core.logic))

(defrel award-received subj obj)

(defrel main-type-gnd subj obj)

(defrel part-of subj obj)

(defrel subclass-of subj obj)

(defrel commons-category subj obj)

(defrel named-after subj obj)

(defrel member-of subj obj)

(defrel foundation/creation-date subj obj)

(defrel image subj obj)

(defrel lccn-identifier subj obj)

(defrel bnf-identifier subj obj)

(defrel native-language subj obj)

(defrel iso-639-3 subj obj)

(defrel language-family subj obj)

(defrel gost-7.75-97-code subj obj)

(defrel wikimedia-language-code subj obj)

(defrel iso-639-1 subj obj)

(defrel iso-639-2 subj obj)

(defrel bncf-thesaurus subj obj)

(defrel instance-of subj obj)

(defrel writing-system subj obj)

(defrel language-group subj obj)

(defrel language-branch subj obj)

(defrel sudoc-identifier subj obj)

(defrel musicbrainz-artist-id subj obj)

(defrel viaf-identifier subj obj)

(defrel occupation-person subj obj)

(defrel field-of-this-profession subj obj)

(defrel gnd-identifier subj obj)

(defrel date-of-birth subj obj)

(defrel mesh-code subj obj)

(defrel ndl-identifier subj obj)

(defrel mathematics-genealogy-project-identifier subj obj)

(defrel signature subj obj)

(defrel child subj obj)

(defrel isni-iso-27729 subj obj)

(defrel brother subj obj)

(defrel sister subj obj)

(defrel place-of-death subj obj)

(defrel sex subj obj)

(defrel father subj obj)

(defrel mother subj obj)

(defrel alma-mater subj obj)

(defrel country-of-citizenship subj obj)

(defrel place-of-birth subj obj)

(defrel date-of-death subj obj)

(defrel country subj obj)

(defrel coordinate-location subj obj)

(defrel is-in-the-administrative-unit subj obj)

(defrel spouse subj obj)

(defrel iso-3166-1-alpha-2 subj obj)

(defrel locator-map-image subj obj)

(defrel iso-3166-1-alpha-3 subj obj)

(defrel country-calling-code subj obj)

(defrel basic-form-of-government subj obj)

(defrel iso-3166-1-numeric subj obj)

(defrel highest-point subj obj)

(defrel head-of-government subj obj)

(defrel openstreetmap-relation-id subj obj)

(defrel nuts-code subj obj)

(defrel coat-of-arms-image subj obj)

(defrel continent subj obj)

(defrel flag-image subj obj)

(defrel anthem subj obj)

(defrel top-level-domain subj obj)

(defrel head-of-state subj obj)

(defrel capital subj obj)

(defrel shares-border-with subj obj)

(defrel official-language subj obj)

(defrel currency subj obj)

(defrel contains-administrative-division subj obj)

(defrel legislative-body subj obj)

(defrel flag subj obj)

(defrel licence-plate-code subj obj)

(defrel local-dialing-code subj obj)

(defrel type-of-administrative-division subj obj)

(defrel time-zone subj obj)

(defrel german-municipality-key subj obj)

(defrel postal-code subj obj)
